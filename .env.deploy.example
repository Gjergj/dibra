# Copy this file to .env.deploy and replace the placeholder with the project
# deployment JWT returned by the orchestrator project-creation response.
DIBRA_DEPLOY_TOKEN=replace-with-project-deployment-token

# The default reaches an orchestrator API running on the Docker host.
DEPLOY_ENDPOINT=http://host.docker.internal:8080/gettasks
