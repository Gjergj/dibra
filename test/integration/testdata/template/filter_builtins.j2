default={{ undef_var | default("fallback") }}
upper={{ name | upper }}
lower={{ name | lower }}
replace={{ name | replace("World", "Go") }}
length={{ items | length }}
join={{ items | join(",") }}
title={{ name | title }}
trim={{ padded | trim }}
