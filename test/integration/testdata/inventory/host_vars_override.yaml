all:
  vars:
    shared_var: from_all
  children:
    webservers:
      hosts:
        testhost:
          host: localhost
          port: 2222
          user: root
          ssh_pass: rootpass
          become: true
          become_password: rootpass
          shared_var: from_host
      vars:
        shared_var: from_group
