all:
  vars:
    level: all
    all_var: present
  children:
    region_east:
      hosts:
        testhost:
          host: localhost
          port: 2222
          user: root
          ssh_pass: rootpass
          become: true
          become_password: rootpass
      vars:
        level: region_east
        region: east
    production:
      children:
        region_east:
      vars:
        level: production
        env: prod
