all:
  children:
    web:
      hosts:
        testhost:
          host: localhost
          port: 2222
          user: root
          ssh_pass: rootpass
          become: true
          become_password: rootpass
    monitoring:
      children:
        web:
      vars:
        monitored: "true"
    staging:
      children:
        web:
      vars:
        env: staging
