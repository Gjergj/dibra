hosts:
  - name: testhost
    host: localhost
    port: 2222
    user: root
    password: rootpass
    become: true

tasks:
  - name: Import tasks using extra vars
    import_tasks: extra_vars_tasks.yaml
