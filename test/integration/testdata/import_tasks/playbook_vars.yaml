hosts:
  - name: testhost
    host: localhost
    port: 2222
    user: root
    password: rootpass
    become: true

tasks:
  - name: Import with vars
    vars:
      greeting: hello
      target: world
    import_tasks: vars_inherit/tasks_with_vars.yaml
