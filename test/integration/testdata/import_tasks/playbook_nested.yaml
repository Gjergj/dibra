hosts:
  - name: testhost
    host: localhost
    port: 2222
    user: root
    password: rootpass
    become: true

tasks:
  - name: Import nested tasks (level1 imports level2)
    import_tasks: nested/level1.yaml
