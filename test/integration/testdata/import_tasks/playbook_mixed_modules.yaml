hosts:
  - name: testhost
    host: localhost
    port: 2222
    user: root
    password: rootpass
    become: true

tasks:
  - name: Import mixed module tasks
    import_tasks: mixed_modules_tasks.yaml
