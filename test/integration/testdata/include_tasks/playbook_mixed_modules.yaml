hosts:
  - name: testhost
    host: localhost
    port: 2222
    user: root
    password: rootpass
    become: true

tasks:
  - name: Include mixed module tasks
    include_tasks: mixed_modules_tasks.yaml
